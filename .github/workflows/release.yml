name: Release

# Triggered by the v<version> tags pushed by the Promote workflow.
# Validates the tag, builds the docker image, pushes it to AWS ECR and then
# sends a deployment message to SQS only after the push step succeeds.
#
# This workflow never bumps a version and never runs the check suite - the tag
# only exists because Promote passed both.
#
# Runner: this repository has no self-hosted runner registered, so every job
# runs on the GitHub-hosted `ubuntu-latest`. That runner is not the deployment
# host and has no deploy script on it, so deployment is requested through SQS -
# there is no local deploy job. When a trusted self-hosted runner is registered, swap
# `runs-on` and add the deploy job described in
# ai-blueprint/skills/devops/github-actions/template-release.yml.
#
# The ECR repository defaults to the GitHub repository name (`api-shelf`). This
# project publishes into `api-book`, so set the `ECR_REPOSITORY` repository
# variable to that value.

on:
  push:
    tags:
      - 'v*'

# Two tags pushed in quick succession must not race on the :latest tag, so
# releases run one at a time and none is cancelled.
concurrency:
  group: release
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  build-and-push:
    name: Build and Push to ECR
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -eo pipefail {0}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Resolve app name, version and repository
        id: meta
        env:
          ECR_REPOSITORY_OVERRIDE: ${{ vars.ECR_REPOSITORY }}
        run: |
          APP="${GITHUB_REPOSITORY##*/}"
          VERSION="${GITHUB_REF_NAME#v}"

          if ! [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+([-+][0-9A-Za-z.-]+)?$ ]]; then
            echo "::error::Tag '$GITHUB_REF_NAME' is not a v<semver> tag"
            exit 1
          fi

          {
            echo "app=$APP"
            echo "version=$VERSION"
            echo "repository=${ECR_REPOSITORY_OVERRIDE:-$APP}"
          } >> "$GITHUB_OUTPUT"

          echo "Releasing $APP:$VERSION"

      # A mistyped tag must not publish an image whose manifest carries another
      # number: the container reports package.json's version at runtime.
      - name: Verify package.json carries the tagged version
        env:
          VERSION: ${{ steps.meta.outputs.version }}
        run: |
          MANIFEST_VERSION="$(
            grep -oE '"version"[[:space:]]*:[[:space:]]*"[^"]+"' package.json \
              | head -n 1 | grep -oE '[0-9]+\.[0-9]+\.[0-9]+'
          )"

          if [ "$MANIFEST_VERSION" != "$VERSION" ]; then
            echo "::error::package.json is at ${MANIFEST_VERSION:-unset} but the tag says $VERSION"
            exit 1
          fi

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-central-1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build the image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: ${{ steps.meta.outputs.repository }}
          IMAGE_TAG: ${{ steps.meta.outputs.version }}
        run: |
          docker build \
            -t "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" \
            -t "$ECR_REGISTRY/$ECR_REPOSITORY:latest" \
            .

      - name: Push the image to Amazon ECR
        id: push-ecr
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: ${{ steps.meta.outputs.repository }}
          IMAGE_TAG: ${{ steps.meta.outputs.version }}
        run: |
          docker push "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"
          docker push "$ECR_REGISTRY/$ECR_REPOSITORY:latest"

      - name: Send deployment message to SQS
        if: ${{ success() && steps.push-ecr.outcome == 'success' }}
        env:
          ECR_REPOSITORY: ${{ steps.meta.outputs.repository }}
          IMAGE_TAG: ${{ steps.meta.outputs.version }}
        run: |
          MESSAGE_BODY="$(jq -cn \
            --arg repo "$ECR_REPOSITORY" \
            --arg version "$IMAGE_TAG" \
            '{repo: $repo, version: $version}')"

          aws sqs send-message \
            --region eu-central-1 \
            --queue-url "https://sqs.eu-central-1.amazonaws.com/069691036768/deploy-ecr.fifo" \
            --message-group-id "$ECR_REPOSITORY" \
            --message-deduplication-id "${ECR_REPOSITORY}-${IMAGE_TAG}" \
            --message-body "$MESSAGE_BODY" \
            --output text --query MessageId

      - name: Summary
        env:
          ECR_REPOSITORY: ${{ steps.meta.outputs.repository }}
          IMAGE_TAG: ${{ steps.meta.outputs.version }}
        run: |
          {
            echo "### Pushed \`${ECR_REPOSITORY}:${IMAGE_TAG}\` to ECR"
            echo ""
            echo "- Also tagged \`latest\`"
            echo "- Deployment message sent to SQS"
          } >> "$GITHUB_STEP_SUMMARY"
